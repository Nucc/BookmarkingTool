== Bookmarking tool

Very simple application to store my web bookmarks.

== Install

Do the next steps to install the application to your local machine:

1. bundle install
2. rake db:migrate
3. rails server
